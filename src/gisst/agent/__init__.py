"""The agent core: the Claude runtime (CLI + API backends), the two-layer prompt
engine, the per-user work queue, session persistence, and the Notion save-marker
parser.
"""

from __future__ import annotations
